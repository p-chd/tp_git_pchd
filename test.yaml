name: Test Runner

on:
  push:
    branches:
      - main

jobs:
  test-runner:
    runs-on:
      - self-hosted
      - Windows
      - x64
    steps:
      - uses: actions/checkout@v2
      - name: Run Tests
        run: |
          echo "Running tests..."